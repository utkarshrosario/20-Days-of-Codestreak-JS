for (var counter = 1; counter <= 50; counter++){
    
    var not_Prime = false;
    for ( var i = 2; i <= counter; i++) {
        if (counter%i==0 && i!==counter) {
            not_Prime = true;
        }
    }
    if (not_Prime === false){
        console.log(counter);
    }
}
